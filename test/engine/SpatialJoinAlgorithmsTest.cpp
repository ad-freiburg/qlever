#include <gmock/gmock.h>
#include <gtest/gtest.h>

#include "engine/SpatialJoinAlgorithms.h"

namespace {  // anonymous namespace to avoid linker problems
    TEST(SpatialJoinAlgorithms, test) {
        ASSERT_TRUE(true);
    }
}